scrape wire magazine to nice pdf

PREREQUISITES

  - bun
  - pdfimages (from poppler)
  - img2pdf

USAGE

  1. login to exacteditions.com

  2. get cookies for "ee_session" and "_reader_session" and set them as
     EE_SESSION and READER_SESSION env var

  3. by default will save to a "downloads" folder under this project, set DEST
     env var for custom location

  4. install deps

       $ bun install

  5. get issues and id data

       $ bun run getIDs.ts

  6. start scraping

       $ bun run down.ts
